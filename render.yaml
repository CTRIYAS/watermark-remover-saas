services:
  - type: web
    name: watermark-remover
    env: python
    plan: free
    # Use default region; adjust if necessary
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app:app --host 0.0.0.0 --port $PORT
